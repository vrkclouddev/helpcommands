to install git: yum install git
to find out the git version: git --version 


git config --global user.name "your name"
git config --global user.email "your email"
git config --global core.editor vim
git config --global core.compression 2 
git config --global diff.tool vim.diff


git config -l 

git log --oneline

git remote add origin https://github.com/USER/REPO.git

git remote add origin https://github.com/vrkclouddev/helpcommands.git
git branch -M main
git push -u origin main

git remote -v
git branch -a 

 
